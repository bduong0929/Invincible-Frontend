
export default class Size {
    id: string;
    size: string;
    quantity: number;

    constructor(id: string, size: string, quantity: number) {
        this.id = id;
        this.size = size;
        this.quantity = quantity;
    }
}